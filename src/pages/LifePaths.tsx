import { PlaceholderPage } from "../components/layout/PlaceholderPage";

export default function LifePathsPage() {
  return (
    <PlaceholderPage
      title="Life Paths"
      description="Life Paths stays parked as a future long-form identity system. It is intentionally hidden from the main navigation until its progression and rewards are ready."
      bullets={[
        "Origin, heritage, and major reputation arcs will eventually live here.",
        "The route remains available for development and direct testing.",
      ]}
    />
  );
}
